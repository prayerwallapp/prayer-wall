# UI Surface Audit — 2026-07-10

Enumerated from `/app` and `/components` directly. Status criteria:

- **MIGRATED** — uses semantic token utilities (`text-primary`, `bg-card`, `border-border`, `bg-brand`, `bg-prayer-bg`, `font-display`, `text-h1`, `shadow-card`, etc.); no zinc-\* or raw `var(--brand-color)` remaining
- **PARTIAL** — mix of token utilities and legacy zinc-\* or `var(--brand-color)` inline
- **NOT_MIGRATED** — entirely on legacy zinc-\* or hardcoded colors; no token utilities present
- **HAS_DESIGN** — a corresponding Figma component or shell exists in `utGO9go3xjfNUC0N6yIbzM`
- **NO_DESIGN** — no Figma equivalent; do not freelance a design without Josiah's direction

---

## Inventory

| Surface | File(s) | Figma | Migration | Notes |
|---|---|---|---|---|
| **Wall page + WallWithModal header** | `app/(wall)/page.tsx`, `components/wall/WallWithModal.tsx` | HAS_DESIGN (Wall Grid shell) | **MIGRATED ✓** | Session 11 Step 1a: submit button → `rounded-full bg-brand text-brand-on`; h1 → `font-display text-h1 text-primary`; subtitle → `text-caption text-muted`; Sign in link → `text-secondary hover:text-primary`; main → `bg-page`. |
| **WallGrid** | `components/wall/WallGrid.tsx` | HAS_DESIGN (Wall Grid shell) | PARTIAL | Size prop wired from Session 9; columns layout not yet token-migrated. |
| **SubmissionCard** | `components/wall/SubmissionCard.tsx` | HAS_DESIGN (Post Card — all 6 variants) | **MIGRATED ✓** | Fully tokenized in Session 9. Prayer/praise semantic colors, accent bar, burst refs preserved, size variants. |
| **SubmissionModal** | `components/wall/SubmissionModal.tsx` | HAS_DESIGN (Submission Composer) | **MIGRATED ✓** | Tokenized + composer restructure in Session 9. SignInStep, SuccessStep on tokens. |
| **ProfileMenu (dropdown)** | `components/wall/ProfileMenu.tsx` | **HAS_DESIGN** | NOT_MIGRATED | `zinc-100/50/900/400` throughout. Avatar uses `var(--brand-color)` for initials bg. Trigger focus ring uses `var(--brand-color)`. Dropdown background, divider, menu items all zinc. **Session 10 Step 1 target.** |
| **ProfileModal (edit profile sheet)** | `components/wall/ProfileModal.tsx` | **NO_DESIGN** | NOT_MIGRATED | Full profile editing: photo upload (with cache-buster), display name, email (read-only), change-password (email accounts only — re-auths before updating), notification toggles (email + in-app), GDPR data download + account deletion. Extensive real logic. No Figma equivalent — Josiah to decide direction. |
| **NotificationBell** | `components/notifications/NotificationBell.tsx` | **NO_DESIGN** | NOT_MIGRATED | Real-time via Supabase subscription; mark-all-read on open; unread badge uses `var(--brand-color)`. Dropdown on zinc. No Figma component. |
| **Landing page** | `app/(marketing)/landing/page.tsx` | **NO_DESIGN** | NOT_MIGRATED | Marketing surface at root domain. `zinc-50/100/200/400/500/600/900` everywhere; accent spots use `var(--brand-color)` inline. Figma file covers app surfaces, not marketing page. |
| **WaitlistForm** | `app/(marketing)/landing/WaitlistForm.tsx` | **NO_DESIGN** | NOT_MIGRATED | Success/error states, POST to `/api/waitlist`. All zinc. |
| **Privacy Policy** | `app/(marketing)/privacy-policy/page.tsx` | **NO_DESIGN** | N/A — legal text page, styling not a priority |
| **Terms of Service** | `app/(marketing)/terms-of-service/page.tsx` | **NO_DESIGN** | N/A — legal text page, styling not a priority |
| **Onboarding** | `app/onboarding/page.tsx`, `OnboardingForm.tsx` | HAS_DESIGN (Onboarding shell) | **MIGRATED ✓** | Session 11 Step 1b: h1 → `font-display text-h2 font-medium text-primary`; subtitle → `text-body-sm text-muted`; input → `border-border focus:ring-brand`; error → `text-danger`; button → `rounded-full bg-brand text-brand-on`. |
| **Submit (redirect)** | `app/submit/page.tsx` | N/A | N/A — single `redirect()` call, no UI |
| **Auth callback** | `app/auth/callback/route.ts` | N/A | N/A — API route, no UI |
| **Auth reset form** | `app/auth/reset/reset-form.tsx` | **NO_DESIGN** | **MIGRATED ✓** | Session 12: h1 → font-display text-h1 text-primary; labels → text-body-sm text-primary; inputs → border-border focus:ring-brand; errors → text-danger; button → rounded-full bg-brand text-brand-on. |
| **Admin layout / sidebar nav** | `app/admin/layout.tsx`, `components/admin/AdminNav.tsx` | **NO_DESIGN** | **MIGRATED ✓** | Session 11 Step 1d: extracted to `AdminNav` client component for `usePathname` active state. aside → `border-border bg-card`; church label → `text-caption text-muted tracking-widest`; active → `bg-page text-brand`; inactive → `text-secondary hover:bg-page hover:text-primary`. |
| **Admin moderation inbox** | `app/admin/page.tsx` | HAS_DESIGN (Moderation Inbox shell) | **MIGRATED ✓** | Session 11 Step 2: h1 → `font-display text-h1 text-primary`; cards → flex with 4px prayer/praise accent bar; badges → `bg-danger-bg/warning-bg text-danger-text/warning-text`; content → `text-body-sm text-primary`; flagged → `text-warning`; timestamps → `text-caption text-muted`. |
| **StatusTabs** | `components/admin/StatusTabs.tsx` | NO_DESIGN (admin UI sub-component) | **MIGRATED ✓** | Session 11 Step 2: active → `border-primary text-primary` (was brand); count inline "(3)" (was separate badge); inactive → `text-muted hover:text-secondary`; border → `border-border`. |
| **ModerationActions** | `app/admin/moderation-actions.tsx` | **NO_DESIGN** | **MIGRATED ✓** | Session 11 Step 2: buttons → `rounded-full`; Approve → `bg-success text-success-on`; Hold → `bg-warning text-warning-on`; Reject → `bg-danger text-danger-on`; sub-flow → `bg-success-bg border-border`; Cancel → `border-border text-secondary`. |
| **RequeueButton** | `app/admin/requeue-button.tsx` | **NO_DESIGN** | **MIGRATED ✓** | Session 11 Step 2: `rounded-full border-border text-secondary hover:bg-page`. |
| **Admin keyword rules** | `app/admin/moderation/page.tsx` | **NO_DESIGN** | **MIGRATED ✓** | Session 12: h1/p → tokens; inputs → border-border focus:ring-brand; Add rule → rounded-full bg-brand; Escalate label → text-danger; Hold label → text-warning; keyword chips → bg-card border-border; rows → border-border bg-card. |
| **Admin settings / branding** | `app/admin/settings/page.tsx` | HAS_DESIGN (Branding Settings Modal) | PARTIAL | BrandingForm section fully migrated (Session 10). Form inputs/fieldsets/save button still on zinc-\*. Pro features section added (Session 12): shows UpgradePrompt for Display App + AI Moderation when plan=free. Full migration deferred. |
| **Admin digest + escalation contacts** | `app/admin/digest/page.tsx` | **NO_DESIGN** (digest settings page; email template is in Figma) | **MIGRATED ✓** | Session 12: all zinc/inline-style → tokens; h1/h2 → font-display text-h1/h2 text-primary; buttons → rounded-full bg-brand; rows → border-border bg-card; Remove → text-danger. |
| **Display page shell** | `app/display/page.tsx` | HAS_DESIGN (Display App 1920×1080) | **MIGRATED ✓** | Session 11 Step 1c: `bg-[var(--background-color)]` → `bg-page`; h1 → `font-display text-h1 font-semibold text-primary`; subtitle → `text-body text-muted`. Error/upgrade states retain `bg-zinc-900` (intentional TV dark states). |
| **DisplayClient** | `app/display/DisplayClient.tsx` | HAS_DESIGN (Display App) | **MIGRATED ✓** | Delegates card rendering to SubmissionCard size="display". Real-time subscription + auto-scroll logic intact. |
| **Embed wall** | `app/(embed)/wall/page.tsx`, `components/wall/SubmissionsGrid.tsx` | **NO_DESIGN** (embed variant not in Figma) | **MIGRATED ✓** | Session 12: removed redundant CSS var injection (root layout handles it); bg-page; CTA → rounded-full bg-brand; SubmissionsGrid → border-border bg-card shadow-card; type badge → bg-prayer-bg/bg-praise-bg; hardcoded label strings fixed to use labels prop. |
| **Weekly digest email template** | `emails/` | HAS_DESIGN (Weekly Digest Email shell) | NOT_AUDITED — not read this session |
| **Error boundaries / 404** | `app/not-found.tsx`, `app/error.tsx` | **NO_DESIGN** | **MIGRATED ✓** | Session 12: created both files. not-found.tsx: bg-page, font-display h1, text-muted, rounded-full bg-brand CTA. error.tsx: 'use client', same token treatment, reset() button. Verified: real 404 renders correctly via HTTP. |
| **Empty states** | Inline in admin inbox, keyword rules, digest, embed wall | **NO_DESIGN** | **MIGRATED ✓** | All migrated as part of each surface's token pass (Sessions 11–12). `text-zinc-500` → `text-body-sm text-muted` throughout. |
| **Toast / notifications system** | None found | **NO_DESIGN** | NOT_BUILT — no toast infrastructure; errors surface inline only. Deferred. |
| **Upgrade prompts** | `components/UpgradePrompt.tsx` | **NO_DESIGN** | **MIGRATED ✓** | Session 12: reusable component (feature + description props). Wired into admin/analytics (hard Pro gate) and admin/settings (soft gate for Display App + AI Moderation). Display page dark-screen state kept bespoke (intentional TV context). |

---

## Surfaces flagged NO_DESIGN — need Josiah's direction

These exist in code but have no Figma counterpart. Do not style them without explicit direction:

1. **ProfileModal** (edit profile sheet) — substantial real functionality (photo upload, name edit, password change, notification prefs, GDPR data export + account deletion). Needs a Figma design before a token pass makes sense.
2. **NotificationBell + dropdown** — real-time subscription, mark-read logic. No Figma spec.
3. **Landing page + WaitlistForm** — marketing surface with its own aesthetic. Different design context than the app.
4. **Toast/notification system** — doesn't exist; needs a design and build decision.

*Removed from list (Session 12): Admin keyword rules, Admin digest settings, Auth reset form, Embed wall, Error boundaries/404, Upgrade prompts — all done with existing tokens.*
*Removed from list (Session 11): Admin sidebar nav, Admin moderation actions.*

---

## Summary by priority

| Priority | Surface | Reason |
|---|---|---|
| **This session** | ProfileMenu | HAS_DESIGN, small component, unblocks wall header token migration |
| **This session** | Admin settings / branding | HAS_DESIGN, prerequisite for Step 2/3 schema + modal work |
| **Next session** | WallWithModal header | HAS_DESIGN (Wall Grid shell), small JSX surface |
| **Next session** | Onboarding | HAS_DESIGN, simple form |
| **Next session** | Display page shell | HAS_DESIGN, shell chrome only |
| **After Figma design** | ProfileModal | NO_DESIGN — get a Figma spec first |
| **After product decision** | Admin status colors | Need semantic status tokens defined |
| **Deferred** | Landing, legal pages, auth reset, admin sidebar | NO_DESIGN or low-priority |
